---
title: Deploy an App Without DevOps on Varity
description: Deploy an app without DevOps from the Dashboard or CLI. Choose a repo, set environment variables, review the fixed monthly price, and publish.
---

import { Steps, Card, CardGrid } from '@astrojs/starlight/components';
import PageMeta from '../../../components/PageMeta.astro';

<PageMeta
  author="Varity Team"
  authorRole="Core Contributors"
  lastUpdated="June 2026"
  stability="stable"
/>

Deploy your application to Varity from the Dashboard, CLI, or MCP server.

:::note[Guided beta]
Varity is in guided beta. Deployments run on production infrastructure with real data, and every account is isolated. There are no automated backups, no high availability, and no point-in-time recovery yet. Keep your own backups for anything you cannot afford to lose.
:::

From the Dashboard, choose a GitHub repository, add environment variables, review the monthly maximum for the reserved hardware, and deploy.

## Three Ways In

Varity accepts three kinds of input, from the CLI, the Developer Portal, or the MCP server in your AI coding tool:

| Input | How | Best for |
|-------|-----|----------|
| **Your project / GitHub repo** | `varitykit app deploy` (this page) | Apps in a [supported framework](/deploy/supported-frameworks) with automatic detection and builds |
| **A Docker image** | `varitykit app deploy --image <ref>`. See [Deploy a Docker Image](/deploy/deploy-docker-image) | Anything already containerized, any language |
| **A template** | The portal gallery. See [agent templates](/tutorials/deploy-ai-agent#one-click-agent-templates) | One-click AI agent and tool deployments |

The rest of this page covers the first path: deploying your own project.

## Quick Deploy

<Steps>

1. **Install the CLI**

   ```bash
   pipx install varitykit
   varitykit login
   ```

2. **Build your project**

   ```bash
   npm run build
   ```

3. **Deploy**

   ```bash
   varitykit app deploy
   ```

4. **Your app is live!**

   Varity will output your live URL:

   ```
   Deploying via Varity…
   ✓ Live: https://your-app.varity.app
   ```

</Steps>

## What Happens During Deployment

When you run `varitykit app deploy`:

1. **Detects your framework:** automatically identifies Next.js, React, Vue, etc.
2. **Builds your app:** runs your build command (`npm run build`)
3. **Uploads your files:** stores them securely
4. **Makes your app live:** generates a public URL
5. **Returns a live URL:** static apps use `https://varity.app/<name>/`; dynamic apps use `https://<name>.varity.app/`

All of this happens automatically.

## Hosting Options

Choose the right hosting for your application type:

<CardGrid>
  <Card title="Static Sites" icon="document">
    **Best for**: React, Vue, Next.js static exports, documentation, landing pages

    **Features**:
    - Global CDN distribution
    - Permanent deployment URLs
    - Automatic SSL certificates
    - Free for verified accounts, up to 3 active static sites and 1 GB of pinned assets

    ```bash
    varitykit app deploy
    ```
  </Card>
  <Card title="Dynamic Apps">
    **Best for**: Full-stack apps, APIs, real-time applications, databases

    **Features**:
    - Container-based compute
    - Managed container hosting
    - Auto-wired database support
    - Priced up to a fixed monthly maximum, prorated by running time

    ```bash
    varitykit app deploy --hosting dynamic
    ```
  </Card>
</CardGrid>

## Before You Deploy

### Environment Variables

Credentials are managed automatically by Varity. Optionally set your app ID:

```bash title=".env.local"
# Optional: Your Varity App ID
NEXT_PUBLIC_VARITY_APP_ID=<your-app-id>

```

See [Environment Variables](/deploy/env-variables) for the complete list and [Managed Credentials](/deploy/managed-credentials) for how credential auto-provision works.

### Test Locally First

Always test your build works locally:

```bash
npm run build
npm run start
```

Visit `http://localhost:3000` and verify everything works.

## Manage Your Deployments

### List All Deployments

View your deployment history:

```bash
varitykit app list
```

Output:
```
Your Deployments (2 of 2)

Name      Type     URL                          Deployed
my-app    dynamic  https://my-app.varity.app    2026-06-02 14:31
my-site   static   https://varity.app/my-site   2026-05-28 09:02
```

The list is read from Varity, so the same deployments appear from any machine.

### Recover a Known-Good Version

To recover after a bad deploy, redeploy a known-good version of your app. Check out the working commit and run `varitykit app deploy` again under the same `--name`. The deployment at that URL is replaced with the version you just built.

### View Deployment Details

Check the details of a specific deployment by app name:

```bash
varitykit app info my-app
```

### Check Deployment Status

View overall deployment status:

```bash
varitykit app status
```

## Deployment URL

Every deployment gets a public URL. URL format depends on hosting type:

```bash
varitykit app deploy --name my-app
# Static hosting → https://varity.app/my-app/
# Dynamic hosting → https://my-app.varity.app/
```

See [App URLs & Subdomains](/deploy/custom-domains) for naming rules and the current status of bring-your-own domains.

:::note[Bring your own domain]
Pointing `myapp.com` at a Varity deployment via DNS is supported. See [App URLs & Subdomains](/deploy/custom-domains/) for the full flow.
:::

## Troubleshooting

### "Deployment failed: Build error"

1. Verify your build works locally:
   ```bash
   npm run build
   ```

2. Check the build output directory exists:
   - Next.js: `.next/` or `out/`
   - React: `dist/` or `build/`
   - Vue: `dist/`

### "Environment variables not found"

Credentials are auto-provided. If you see this error, run `varitykit doctor` to check your setup.

### "Deployment timed out"

Check your internet connection:

```bash
varitykit doctor
```

If issues persist, you can preview what would deploy without deploying:

```bash
varitykit app deploy --dry-run
```

## Advanced Configuration

### Deploy Options

| Option | Default | Description |
|--------|---------|-------------|
| `--hosting` | `auto` | Hosting type: `auto`, `static`, or `dynamic` (auto picks based on your project) |
| `--name` | from `package.json` | App name (and URL) for this deployment |
| `--path` | `.` | Project directory |
| `--mode` | auto | Override the detected build/run mode |
| `--skip-build` | off | Skip the build step (deploy a pre-built output) |
| `--repo-url` | None | Deploy directly from a GitHub repo URL instead of a local path |
| `--dry-run` | off | Simulate the deploy: show what would deploy without deploying (not supported with `--image`) |

### Deploy a prebuilt Docker image

Instead of a project directory, you can deploy an existing image. See [Deploy a Docker Image](/deploy/deploy-docker-image) for the full flow.

| Option | Default | Description |
|--------|---------|-------------|
| `--image` | None | Image reference to deploy as-is (e.g. `ghcr.io/acme/api:1.4.2`) |
| `--port` | `80` | Container port your app listens on |
| `--image-registry` / `--image-username` / `--image-password` | None | Credentials for a private registry |


## Next Steps

- [Configure environment variables](/deploy/env-variables)
- [CLI Reference](/cli/overview)
- [Quick Start Guide](/getting-started/quickstart)
- [Troubleshooting](/resources/troubleshooting)
