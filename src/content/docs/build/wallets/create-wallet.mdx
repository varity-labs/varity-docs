---
title: Create Account
description: Varity automatically creates secure accounts when users sign in. No manual setup required.
---

import { Aside } from '@astrojs/starlight/components';
import Accordion from '../../../../components/Accordion.astro';
import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta
  author="Varity Team"
  authorRole="Core Contributors"
  lastUpdated="March 2026"
  stability="stable"
/>

# Create Account

Accounts are created automatically when users sign in. No manual setup is needed.

## How Account Creation Works

When a user signs in through `PrivyStack`, Varity automatically:

1. **Authenticates the user** via email or social login
2. **Creates a secure account** linked to the user's identity
3. **Manages account credentials** -- encrypted and recoverable, nothing for users to remember

Users see a familiar sign-in experience, just like any other modern app.

## Displaying User Profile

Use the pre-built `PrivyUserProfile` component for a ready-made profile display:

```tsx title="src/components/Profile.tsx"
import { PrivyUserProfile } from '@varity-labs/ui-kit';

function ProfilePage() {
  return (
    <div>
      <h2>Your Profile</h2>
      <PrivyUserProfile />
    </div>
  );
}
```

## Accessing User Info

After authentication, use `usePrivy` to access the current user's details:

```tsx title="src/components/AccountInfo.tsx"
import { usePrivy } from '@varity-labs/ui-kit';

function AccountInfo() {
  const { authenticated, user, ready } = usePrivy();

  if (!ready) return <p>Loading...</p>;
  if (!authenticated) return <p>Please sign in first</p>;

  return (
    <div>
      <p>User ID: {user?.id}</p>
      <p>Email: {user?.email?.address || 'Not provided'}</p>
      <p>Name: {user?.google?.name || user?.twitter?.name || 'Not provided'}</p>
    </div>
  );
}
```

## Authentication Methods

Users can create accounts through any supported login method:

| Method | Description |
|--------|-------------|
| **Email** | Magic link sent to email, no password needed |
| **Google** | Sign in via Google account |
| **Twitter** | Sign in via Twitter/X account |
| **Discord** | Sign in via Discord account |
| **GitHub** | Sign in via GitHub account |

All methods result in a secure account being created automatically.

## Checking Authentication State

```tsx title="src/components/AuthGate.tsx"
import { usePrivy } from '@varity-labs/ui-kit';

function AuthGate({ children }) {
  const { ready, authenticated } = usePrivy();

  if (!ready) {
    return <p>Initializing...</p>;
  }

  if (!authenticated) {
    return <p>Please sign in to continue.</p>;
  }

  return <>{children}</>;
}
```

## Signing Out

```tsx
import { useLogout } from '@varity-labs/ui-kit';

function SignOutButton() {
  const { logout } = useLogout();

  return <button onClick={logout}>Sign Out</button>;
}
```

<Accordion title="Advanced: Account Infrastructure">

Varity automatically manages account infrastructure. For advanced use cases, see [Session Keys](/build/wallets/session-keys).

</Accordion>

## Next steps

- [Accounts Quick Start](/build/wallets/quickstart) -- Overview of accounts
- [Session Keys](/build/wallets/session-keys) -- Advanced operation patterns
- [Authentication Quick Start](/build/auth/quickstart) -- Set up login
